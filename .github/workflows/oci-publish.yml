name: oci-publish

# Render + Lint + Scan on PR; render + push + sign on tag push.
#
# Tag pattern: <sub-layer>/<component>-vX.Y.Z
#   e.g. lifecycle/crossplane-v0.1.0, observability/loki-v0.3.1
# The component is the OCI distribution unit (ADR-0009); the sub-layer is the
# organizational bracket. The workflow extracts both from the tag and
# publishes exactly this one component as a signed OCI artifact.
#
# Convention (AGENTS.md § CI conventions):
#   1. Devbox cache active (jetify-com/devbox-install-action)
#   2. Locally reproducible via `task ci` and `task publish -- <sub-layer>/<component> <tag>`
#   3. Pipeline = thin task caller — steps call exclusively `task <name>`

on:
  pull_request:
    branches: [main]
  # merge_group: the merge queue runs the required `ci` check against the projected
  # merge result (a `refs/heads/gh-readonly-queue/…` ref). The `publish` job below
  # stays tag-gated, so a merge_group event runs `ci` only — never publish/sign.
  merge_group:
  push:
    tags:
      # One pattern per sub-layer (AGENTS.md taxonomy). The capability-driven
      # sub-layers (compute/identity/network/security/storage-block) are listed
      # too so a `<sub-layer>/<component>-vX.Y.Z` tag triggers publish the moment
      # the sub-layer exists — patterns for not-yet-created dirs are harmless.
      - 'automation/*-v*'
      - 'compute/*-v*'
      - 'databases/*-v*'
      - 'identity/*-v*'
      - 'lifecycle/*-v*'
      - 'observability/*-v*'
      - 'network/*-v*'
      - 'registry/*-v*'
      - 'secrets/*-v*'
      - 'security/*-v*'
      - 'storage-block/*-v*'
      - 'storage-objects/*-v*'

permissions:
  contents: read

jobs:
  # --------------------------------------------------------------------
  # PR trigger: only Lint + Render + Conftest. No push, no sign.
  # --------------------------------------------------------------------
  ci:
    # Runs on PRs AND on merge_group (the queue re-validates the projected merge tree).
    if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
    runs-on: ubuntu-latest
    # PR CI may cancel a superseded run on the same PR ref (resource hygiene).
    concurrency:
      group: oci-publish-ci-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v4
        with:
          # fetch-depth: 0 so all git tags are present: `task ci` → validate:release-config
          # guard E asserts every released (tagged) component is in the manifest, and a
          # tag-less shallow checkout would blind it (it fails loud rather than vacuously). #318
          fetch-depth: 0

      - name: Devbox setup
        uses: jetify-com/devbox-install-action@8c6a66ed6273138b1915457069de78cb52fe3bd7  # v0.15.0
        with:
          enable-cache: true

      # Cache the openknowledge CLI (task okf:install, the one non-devbox tool — see
      # AGENTS.md § CI conventions Tooling carve-out) so a normal PR restores it instead
      # of re-downloading from the third-party GitHub release on every run. The download
      # itself is checksum-verified + retried in the task; the cache removes the live-network
      # dependency of the required `ci` gate on all but a version-bump / cache-miss run.
      # The key is the Taskfile.yml hash, so any Taskfile edit rotates it; the prefix
      # restore-keys then still restores the most-recent okf-<os>-* entry (fuzzy hit). That
      # is safe because okf:install trusts a restored binary only when its sha256 matches
      # the in-repo bin_sha pin AND its version matches OKF_VERSION (#522) — a poisoned or
      # version-skewed entry fails that gate and re-downloads from the checksum-pinned asset.
      - name: OKF CLI cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: .okf
          key: okf-${{ runner.os }}-${{ hashFiles('Taskfile.yml') }}
          restore-keys: |
            okf-${{ runner.os }}-

      - name: Reproduce local CI pipeline
        run: devbox run -- task ci

  # --------------------------------------------------------------------
  # Tag push: render + push + sign for the tagged sub-layer.
  # --------------------------------------------------------------------
  publish:
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    # Serialize publishes of the SAME tag ref; NEVER cancel a mid-flight publish — a
    # cancelled run can leave a pushed-but-unsigned (or unverified) OCI artifact.
    concurrency:
      group: oci-publish-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      contents: read
      packages: write   # GHCR push
      id-token: write   # cosign keyless OIDC
    steps:
      # No fetch-depth: 0 here — the publish job runs `task ci:artifact`, which does NOT
      # include the release-coverage guard (validate:release-config / guard E), so it does
      # not need the full tag set. Tags would only matter for guard E, which is a PR-gate
      # concern, not a per-publish one (#318 review).
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v4

      - name: Devbox setup
        uses: jetify-com/devbox-install-action@8c6a66ed6273138b1915457069de78cb52fe3bd7  # v0.15.0
        with:
          enable-cache: true

      - name: Derive component ID and tag from refs/tags
        id: parse
        shell: bash
        run: |
          set -eu
          # tag is e.g. "lifecycle/crossplane-v0.1.0"
          full="${GITHUB_REF_NAME}"
          # component ID = everything before the last "-v" (e.g. "lifecycle/crossplane")
          component_id="${full%-v*}"
          # version tag = "v" + rest (e.g. "v0.1.0")
          version="v${full##*-v}"
          # Both parsed fields flow verbatim into the signed `task publish`/`task
          # verify` steps, so validate BOTH here — independently of git's ref-name
          # grammar — before anything is written to GITHUB_OUTPUT or pushed/signed.
          #
          # 1) component_id: a "<sub-layer>/<component>" identifier in the catalog
          #    charset, then it MUST resolve to a real component dir. The shape check
          #    also blocks shell metacharacters and path traversal; the dir check
          #    catches a malformed tag (or a name that breaks the -v split) before
          #    render/push/sign acts on the wrong path.
          if [[ ! "$component_id" =~ ^[a-z0-9-]+/[a-z0-9-]+$ ]]; then
            echo "::error::component_id '$component_id' (from tag '$full') is not a '<sub-layer>/<component>' identifier" >&2
            exit 1
          fi
          dir="sub-layers/${component_id%/*}/components/${component_id##*/}"
          if [ ! -d "$dir" ]; then
            echo "::error::component_id '$component_id' does not resolve to a component dir ($dir; expected '<sub-layer>/<component>')" >&2
            exit 1
          fi
          # 2) version: a strict vMAJOR.MINOR.PATCH[-prerelease] SemVer. The accepted
          #    set is a SUBSET of the OCI tag grammar — build metadata ("+...") is
          #    rejected because "+" is illegal in an OCI tag, so a version that passes
          #    here cannot fail mid-publish at oras push / cosign sign. This also
          #    rejects an empty/garbage version (e.g. a trailing-"-v" tag → "v") and
          #    any shell metacharacter.
          if [[ ! "$version" =~ ^v(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-[0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::version '$version' (from tag '$full') is not a valid vMAJOR.MINOR.PATCH SemVer tag" >&2
            exit 1
          fi
          echo "component_id=$component_id" >> "$GITHUB_OUTPUT"
          echo "version=$version"           >> "$GITHUB_OUTPUT"
          echo "Tag '$full' → component_id='$component_id', version='$version'"

      - name: "Local CI pipeline (artifact validation: lint + render + crd-split + conftest)"
        # ci:artifact, NOT full ci — the publish job validates the artifact it is about to
        # sign; the catalog-wide release-coverage gates run at the PR gate, not per-publish (#318).
        run: devbox run -- task ci:artifact

      - name: Authenticate oras + cosign against GHCR
        run: |
          # oras (push) and cosign (sign) read/write ~/.docker/config.json —
          # both logins are required, otherwise the oras push or cosign sign fails with
          # "UNAUTHORIZED" on the artifact's pull scope.
          echo "${{ secrets.GITHUB_TOKEN }}" | devbox run -- oras login ghcr.io \
            --username "${{ github.actor }}" \
            --password-stdin
          echo "${{ secrets.GITHUB_TOKEN }}" | devbox run -- cosign login ghcr.io \
            --username "${{ github.actor }}" \
            --password-stdin

      - name: "E5 image-CVE scan (advisory during catalog remediation)"
        # E5 CVE scan of THIS published component's rendered images. Currently
        # ADVISORY (continue-on-error): the baseline shows ~90% of catalog images
        # carry FIXABLE HIGH/CRITICAL CVEs, so a hard block would strand nearly every
        # publish. It runs here as the per-publish measurement while the catalog is
        # remediated component-by-component (tracking epic). FLIP TO HARD by deleting
        # `continue-on-error` once the catalog is green — the scan already fails-closed
        # on HIGH/CRITICAL (and on a scan/infra error), so the flip is a one-line
        # change. As a workflow STEP it is not a required status check, so once hard it
        # avoids the `gh pr merge --admin` bypass a required-check skip allows (a direct
        # GHCR push remains an out-of-band path). Runs AFTER GHCR auth so trivy inherits
        # the docker config for any ghcr.io image; upstream public images pull
        # anonymously. A CRDs-only / config-only component (0 images) publishes clean.
        # Bounded per AC11.
        continue-on-error: true
        timeout-minutes: 15
        env:
          TRIVY_COMPONENT: ${{ steps.parse.outputs.component_id }}
          # Strict E1 (block ALL HIGH/CRITICAL) pinned explicitly. Relaxing to
          # block-fixable-only is a CODEOWNERS-reviewed change to '1' HERE — never a
          # silent GHA repository Settings variable.
          TRIVY_IGNORE_UNFIXED: ''
        run: devbox run -- task scan:trivy-images-of

      - name: task publish (render + push + sign keyless)
        run: |
          devbox run -- task publish -- \
            "${{ steps.parse.outputs.component_id }}" \
            "${{ steps.parse.outputs.version }}"

      - name: Verification (cosign verify)
        run: |
          devbox run -- task verify -- \
            "${{ steps.parse.outputs.component_id }}" \
            "${{ steps.parse.outputs.version }}"
