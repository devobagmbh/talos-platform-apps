components:
  - prometheus-operator-crds  # sync-wave -1 — strict-B CRDs artifact (ADR-0028): the monitoring.coreos.com CRDs, before the operator workload
  - prometheus-operator       # sync-wave 0  — strict-B workload artifact (ADR-0028): the operator controller (kube-prometheus-stack operator-only); CRDs in prometheus-operator-crds
  - kube-prometheus-stack   # sync-wave 0  — legacy operator-only placeholder (disposition tracked in #38; the strict-B CRDs now live in prometheus-operator-crds)
  - loki                    # sync-wave 10 — braucht Garage-Bucket
  - mimir                   # sync-wave 10 — braucht Garage-Bucket
  - tempo                   # sync-wave 10 — braucht Garage-Bucket
  - alloy                   # sync-wave 20 — braucht Loki/Mimir/Tempo-Endpoints
  - grafana                 # sync-wave 20 — Datasources zeigen auf Loki/Mimir/Tempo
  - hubble                  # sync-wave 0  — Cilium network-flow visibility (relay/ui); independent of LGTM, needs the Cilium-agent Hubble server
