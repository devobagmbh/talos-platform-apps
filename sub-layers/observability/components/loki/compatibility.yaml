requires:
  storage-objects/garage: ">=v0.1.0" # S3 backend for chunks
provides:
  - name: loki
    api_surface: [] # filled in with issue #17
    capabilities:
      - {id: logs-storage, swap_class: data-migration}
      - {id: logs-query, swap_class: drop-in}
