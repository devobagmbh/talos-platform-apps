requires:
  storage-objects/garage: ">=v0.1.0" # S3 backend for blocks
provides:
  - name: mimir
    api_surface: [] # filled in with issue #17
    capabilities:
      - {id: metrics-storage, swap_class: data-migration}
