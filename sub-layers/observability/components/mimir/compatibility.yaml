requires:
  storage-objects/garage: ">=v0.1.0" # S3-Backend für Blocks
provides:
  - name: mimir
    apis: [] # wird mit Issue #17 befüllt
    capabilities:
      - {id: metrics-storage, swap_class: data-migration}
