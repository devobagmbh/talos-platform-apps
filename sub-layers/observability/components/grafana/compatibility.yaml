requires:
  observability/loki: ">=v0.1.0"
  observability/mimir: ">=v0.1.0"
  observability/tempo: ">=v0.1.0"
provides:
  - name: grafana
    apis: [] # wird mit Issue #17 befüllt
    capabilities:
      - {id: dashboards, swap_class: label-move}
