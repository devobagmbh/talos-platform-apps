requires: {} # no catalog-internal deps; substrate need is capability-form, not a version pin (#71)
provides:
  - name: valkey-operator
    version:
      sot: app
      app: v0.0.61
    api_surface:
      - hyperspike.io/Valkey@v1
    capabilities:
      - {id: redis-managed, swap_class: data-migration} # realizes redis-managed via Valkey (Redis-wire-compatible)
