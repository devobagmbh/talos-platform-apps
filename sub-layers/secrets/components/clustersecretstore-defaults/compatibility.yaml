requires:
  talos-platform-base: ">=v0.4.0 <v1.0.0"
  secrets/external-secrets: ">=v0.1.0"   # ClusterSecretStore-CRD

provides:
  - name: clustersecretstore-defaults
    apis: []  # wird mit Issue #15a befüllt
