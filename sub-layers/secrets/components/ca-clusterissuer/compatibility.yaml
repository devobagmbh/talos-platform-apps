requires:
  secrets/external-secrets: ">=v0.1.0" # ExternalSecret pulls the CA key from Vault
provides:
  - name: ca-clusterissuer
    api_surface: [] # filled in with the TLS issue
    capabilities:
      - {id: tls-issuance, swap_class: label-move}
