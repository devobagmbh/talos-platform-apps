requires:
  secrets/external-secrets: ">=v0.1.0" # ExternalSecret zieht den CA-Key aus Vault
provides:
  - name: ca-clusterissuer
    apis: [] # wird mit dem TLS-Issue befüllt
    capabilities:
      - {id: tls-issuance, swap_class: label-move}
