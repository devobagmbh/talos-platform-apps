# Customization Contract (ADR-0023). The ESO operator + CRDs are cluster-agnostic
# — no cluster-specific values. ClusterSecretStore/Vault auth + the consumer's
# generators/ExternalSecrets (e.g. the seeder GHCR GithubAccessToken, ADR-0025)
# live in the consumer repos (Layer 3), not here.
schema_version: "1"
component: secrets/external-secrets
cluster_agnostic: true
sync_wave: "0"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies: []
