requires: {} # apps does NOT depend on the substrate (ADR-0009, #71); no base line
provides:
  - name: cert-manager
    version:
      sot: app
      app: v1.20.2
      chart: v1.20.2
    api_surface:
      - cert-manager.io/Certificate@v1
      - acme.cert-manager.io/Challenge@v1
    capabilities:
      - {id: tls-issuance, swap_class: label-move}
