components:
  - external-secrets-crds          # sync-wave -1 — ESO CRDs (strict-B -crds half, ADR-0028)
  - cert-manager                   # sync-wave 0  — TLS-Issuance-Controller + CRDs
  - external-secrets               # sync-wave 0  — CRD-free ESO operator (strict-B workload half)
  - clustersecretstore-defaults    # sync-wave 10 — braucht ESO-CRDs
  - ca-clusterissuer               # sync-wave 20 — cert-manager CA-Issuer, CA-Key via ESO aus Vault
