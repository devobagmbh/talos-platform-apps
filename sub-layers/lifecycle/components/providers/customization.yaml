# Customization Contract (ADR-0023). Provider-/Function-Pakete sind
# cluster-agnostisch (gepinnte Paket-Refs). ProviderConfigs liefert der Konsument.
schema_version: "1"
component: lifecycle/providers
cluster_agnostic: true
sync_wave: "10"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies:
  - lifecycle/crossplane
