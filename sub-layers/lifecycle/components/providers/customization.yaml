# Customization Contract v2 (ADR-0024). Provider/function packages are
# cluster-agnostic (pinned package refs). ProviderConfigs are supplied by the
# consumer repo, so this component provides no refs/selectors and requires nothing.
freeze_line:
  # The signed pre-rendered manifest is the workload DEFAULT (Provider/Function packages),
  # published as a native-OCI Kustomize base (kustomization.yaml + manifest.yaml,
  # `task package` per apps#508) — consumers attach per-cluster overlays via
  # `source.kustomize.patches`. Calibrated-friction (ADR-0024): the base itself
  # restricts nothing; the image digest is pinned at admission (cosign) and
  # dangerous classes are fenced by the consumer-side ADR-0018 Kyverno safe-defaults
  # — never a catalog allowlist here.
  workload: rendered/manifest.yaml
provided_refs: {}
provided_selectors: {}
required:
  env_keys: []
  config_files: []
  secret_keys: []
  selector_crs: []
sync_wave: "10"
external_dependencies:
  - lifecycle/crossplane
