# Customization Contract (ADR-0023). XRD + Composition sind cluster-agnostisch
# (das Template selbst). Die Cluster-Identität kommt zur Laufzeit aus der vom
# Konsumenten gelieferten cluster-config-ConfigMap (ADR-0022), nicht hier.
schema_version: "1"
component: lifecycle/compositions
cluster_agnostic: true
sync_wave: "20"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies:
  - lifecycle/providers
