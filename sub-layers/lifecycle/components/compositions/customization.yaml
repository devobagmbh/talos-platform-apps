# Customization Contract (ADR-0023). XRD + Composition are cluster-agnostic
# (the template itself). Cluster identity comes at runtime from the XCluster
# claim spec authored by the consumer (child repo), not from here.
schema_version: "1"
component: lifecycle/compositions
cluster_agnostic: true
sync_wave: "20"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies:
  - lifecycle/providers
