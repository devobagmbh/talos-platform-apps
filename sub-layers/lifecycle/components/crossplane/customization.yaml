# Customization Contract (ADR-0023). Crossplane-Operator ist cluster-agnostisch
# — keine cluster-spezifischen Werte. ProviderConfigs (mit Cluster-Identität)
# liefert das Konsumenten-Repo separat (talos-seeder-cluster/crossplane/providerconfigs/).
schema_version: "1"
component: lifecycle/crossplane
cluster_agnostic: true
sync_wave: "0"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies: []
