# Customization Contract (ADR-0023). The Crossplane operator is cluster-agnostic
# — no cluster-specific values. ProviderConfigs (carrying cluster identity) are
# supplied separately by the consumer repo (talos-seeder-cluster/crossplane/providerconfigs/).
schema_version: "1"
component: lifecycle/crossplane
cluster_agnostic: true
sync_wave: "0"
required:
  configmap_keys: []
  secret_keys: []
external_dependencies: []
