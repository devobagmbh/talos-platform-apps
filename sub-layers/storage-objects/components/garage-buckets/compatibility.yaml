requires:
  storage-objects/garage: ">=v0.1.0"  # Garage-API
  secrets/external-secrets: ">=v0.1.0"  # ExternalSecret für Access-Keys

provides:
  - name: garage-buckets
    apis: []  # wird mit Issue #13 befüllt
