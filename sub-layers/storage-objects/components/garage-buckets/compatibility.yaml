requires:
  storage-objects/garage: ">=v0.1.0"  # Garage API
  secrets/external-secrets: ">=v0.1.0"  # ExternalSecret for access keys

provides:
  - name: garage-buckets
    api_surface: []  # filled in with issue #13
