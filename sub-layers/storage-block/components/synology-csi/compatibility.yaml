requires: {} # no catalog-internal dependencies (substrate assumptions are capability-form, not version pins — #71)
provides:
  - name: synology-csi
    version:
      sot: app
      app: v1.3.0
    api_surface: []
    capabilities:
      - {id: block-storage-network, swap_class: data-migration}  # Synology DSM iSCSI LUNs (ADR-0026); defined in catalog/capability-index.yaml
