# Sub-layer aggregate (components have their own compatibility.yaml).
components:
  - velero-crds  # sync-wave -1 (strict-B CRDs, ADR-0028)
  - actions-runner-controller-crds  # sync-wave -1 (strict-B CRDs, ADR-0028)
  - velero       # sync-wave 0
# renovate removed (2026-06-10): dependency automation = GitHub Dependabot (native), not a catalog component.
