requires:
  talos-platform-base: ">=v0.4.0 <v1.0.0"
provides:
  - name: velero
    apis: [] # wird mit Issue #16 befüllt
    capabilities: [] # TODO: Capability backup definieren (proposed, Vertrag offen)
