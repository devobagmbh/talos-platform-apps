requires:
  talos-platform-base: ">=v0.4.0 <v1.0.0"
  # Capability deps (#84): Harbor no longer embeds DB/Redis. Postgres comes from
  # the cnpg-postgres capability (databases/cnpg), Redis from redis-managed
  # (databases/valkey-operator). The concrete CNPG `Cluster` + `Valkey` CRs are
  # consumer-owned (seeder); registry blobs stay on synology-csi PVCs (ADR-0026).
  cnpg-postgres: "*"
  redis-managed: "*"
provides:
  - name: harbor
    apis: []  # wird mit Issue #14 befüllt
    capabilities: [] # TODO: Capability oci-registry definieren (proposed, Vertrag offen)
