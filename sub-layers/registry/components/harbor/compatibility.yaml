requires:
  talos-platform-base: ">=v0.4.0 <v1.0.0"
  # Seeder-Pull-Through-Cache-Profil: eingebettete DB + ephemeral Storage.
  # KEIN databases/cnpg, KEIN storage-objects/garage (garage obsolet). Das
  # persistente office-lab-Harbor-Profil (CNPG + S3) bekommt ein eigenes Render-Profil.
provides:
  - name: harbor
    apis: []  # wird mit Issue #14 befüllt
    capabilities: [] # TODO: Capability oci-registry definieren (proposed, Vertrag offen)
