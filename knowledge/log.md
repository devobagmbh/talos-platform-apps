# Bundle Update Log

## 2026-07-22

- **Build-contract hardening (#737)**: `.claude/skills/build-catalog-component/CONVENTIONS.md` gained two new sections — `## Reading rendered artifacts` (a bounded-window reading discipline binding any artifact over 500 lines by **size, not provenance**, so a vendored `manifests/**` CRD file is in scope exactly as a render is: inventory via `^kind:`, then lens-bounded windows, with any entry the agent neither opened nor judged named in its output; an artifact absent at its explicit path is regenerated by a Bash-holding agent, and otherwise recorded as not verifiable in this dispatch through the agent's own contract vocabulary — `not-locally-verifiable` for `catalog-evaluator`, a `not-read:` finding plus `needs-info` for a read-only reviewer — never through a severity or a verdict state this rule invents) and `## Render-grounded claims` (a claim about rendered output or chart behaviour is written only after being read from the render or the chart template, binding the builder's own work, every orchestrator-authored brief, and every render claim the orchestrator itself cites). In `.claude/skills/build-catalog-component/SKILL.md` the two disciplines have deliberately different reach: the **reading discipline** is brief content at all **five** render-inspecting dispatch sites — the Phase-3 evaluator, the Phase-4 fixer, the Phase-5 reviewers, the Phase-5 semantic-bearing-fix re-dispatch, and the Phase-6 step-2 evaluator re-dispatch — so no brief depends on the orchestrator remembering it; the **grounding rule** binds the **two orchestrator-authored implementer briefs** (Phase-4 fixer, Phase-5 semantic-bearing-fix re-dispatch), because it constrains what the orchestrator may assert *when authoring a brief* rather than what a dispatched agent must do, and those two are the briefs the orchestrator authors. Re-verified `workflows/catalog-build-pipeline.md` and `decisions/DR-0001-specification-driven-component-build.md` (both list the edited `CONVENTIONS.md` in `sources:`): purely additive, no present-tense claim falsified; timestamps bumped.

## 2026-07-17

- **Merge-queue model correction**: `reference/ci-and-merge-gates.md` § Merge method now documents that `main` runs the `merge-queue-main` ruleset (`SQUASH`, `ALLGREEN`), so `gh pr merge --squash` (or `--auto --squash`) **enqueues** rather than merging immediately (queue rebuilds against `main`, re-runs required checks on the `merge_group` head, squash-merges when all-green; `--delete-branch` incompatible, head branch not auto-deleted, `--admin` mechanically blocked by the ruleset). Timestamp bumped (`AGENTS.md` in `sources:` was correspondingly corrected in the same change). Mirrors the `AGENTS.md` § Branch protection & merge gates correction and the `pr-gate` skill rework. Oracle bound via `gh api`: repo settings `allow_auto_merge: true`, `delete_branch_on_merge: false`; ruleset `merge-queue-main` active (`gh api .../rulesets`) — `merge_method: SQUASH`, `grouping_strategy: ALLGREEN`, `min/max_entries_to_merge: 1/5`, `bypass_actors: []`, `current_user_can_bypass: never`.

## 2026-07-16

- **Capability registration (#649)**: added the `events-collect` capability to `catalog/capability-index.yaml` (Observability: Events; impl `alloy` active + `otelcol` considered, `swap_class: consumer-change`) — the prerequisite index entry the planned `observability/alloy-singleton` component references. Re-verified the two concepts carrying `capability-index.yaml` in `sources:` (`glossary.md`, `architecture/capability-layer-model.md`): no present-tense claim is affected (neither enumerates the capability set), timestamps bumped for the source touch.

## 2026-07-13

- **Self-maintenance**: added the knowledge-bundle maintenance directive to `AGENTS.md` (§ Knowledge-bundle maintenance) and its mechanical backstop `task okf:freshness` (advisory; a `sources:` change in a PR without a concept `timestamp:` bump is flagged), run by `okf-freshness.yml`. Adopted hand-written from the upstream `openknowledge` `docs` rule; blocking flip tracked in #541.
- **Freshness re-verification**: bumped `reference/sub-layers/secret-management.md` for #524 — added the `vault-config-operator` workload row and resolved the former `vault-config-operator-crds` orphan-half gap note.
- **Review reconciliation (#522)**: corrected the catalog-scale count to **62 components** across the bundle (`vault-config-operator` #524 landed after the initial census); fixed the `vault-config-operator` sync-wave 0→1 in `reference/sub-layers/secret-management.md` (needs `cert-manager` wave 0 Healthy first); refreshed the `storage-block` `democratic-csi`/`synology-csi` rows + Gaps note for the now-defined `block-storage-network` capability (#538/#539). Hardened `task okf:install` to checksum-gate the persisted/GHA-cache-restored binary (was version-string only).

## 2026-07-11

- **Initialization**: created the `knowledge/` Open Knowledge Format v0.1 bundle as a curated orientation layer over the catalog's architecture, contracts, gates, and workflows.
- **Migration**: relocated the repo-local decision record `docs/decisions/0001` verbatim into `decisions/DR-0001`; the `docs/` tree was removed.
- **Spec**: stored a pinned upstream copy of the OKF v0.1 draft in `SPEC.md`.
- **Tooling**: the `openknowledge` CLI (pinned v0.4.0) validates the bundle via `task okf:validate`; a `list --json` link gate hard-fails on unresolved or bundle-escaping links (the CLI's own `validate` reports those as warnings only).
- **Primary-home expansion**: established the bundle as the primary documentation home ([DR-0002](decisions/DR-0002-knowledge-bundle-as-primary-doc-home.md)); added one [reference concept per sub-layer](reference/sub-layers/index.md) (12 sub-layers, 62 components). A catalog gap analysis (documentation, architecture/capability, gate/coverage) is tracked in issue #523.
