---
# Negative fixture: an unknown key INSIDE a tunable_targets item should fail
# additionalProperties:false on the item level (AC2, nested level).
requires: {}
provides:
  - name: test
    api_surface: []
resource_policy:
  class: tunable
  tunable_targets:
    - workload: test
      kind: Deployment
      containers: [test]
      policy: strict   # unknown key — fails additionalProperties:false on the item
