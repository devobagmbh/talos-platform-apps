---
# Positive fixture: a valid rbac_policy (class:narrowable with the role, the golden
# and the declared cost). MUST pass validation — asserted by task
# validate:compatibility.
requires: {}
provides:
  - name: test
    api_surface: []
rbac_policy:
  class: narrowable
  cluster_role: test-manager-role
  baseline: rbac-baseline.triples
  inert_when_namespaced:
    - apiGroups: [""]
      resources: [nodes]
      verbs: [get, list]
