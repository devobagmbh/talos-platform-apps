# fixture: SCHEMA REJECT (kind enum). Otherwise fully gate-valid — 2 real members, bare-name default
# in members, real dirs. Removing/relaxing the kind enum ACCEPTS it (D4).
groups:
  - id: loki
    kind: role-additive
    default: observability/loki
    members: [observability/loki, observability/loki-distributed]
