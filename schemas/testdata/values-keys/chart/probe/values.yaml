# The only key the template reads. A component that overrides it changes the render;
# any other key it sets changes nothing — which is what test:values-keys asserts.
used: fromChartDefaults
extraArgs:
  config.expand-env: "fromChartDefaults"
