# .sops.yaml — template (Issue #3 blocks final activation)
#
# As soon as the five age recipients from Issue #3 (5-recipient SOPS) are
# generated, rename this file to .sops.yaml and replace the <TODO> slots with the
# real public keys. The pattern matches all *.enc.yaml/*.enc.json
# files below sub-layers/, plus top-level .env.enc etc.
#
# 4 human recipients (M1, M2, GF, Safe) + 1 service recipient (Seeder)
# — all equally weighted in terms of encryption order.
#
# Docs: https://github.com/devobagmbh/talos-platform-docs/blob/main/adr/0011-secrets-management.md

creation_rules:
  - path_regex: '\.enc\.(yaml|yml|json|env)$'
    encrypted_regex: '^(data|stringData|password|token|key|secret|cert)$'
    age: |-
      # M1 — Maintainer 1 (Robert Schneider)
      # age1<TODO-fill-after-issue-3>
      ,
      # M2 — Maintainer 2 (to be identified)
      # age1<TODO-fill-after-issue-3>
      ,
      # GF — management
      # age1<TODO-fill-after-issue-3>
      ,
      # Safe — printed air-gap backup key
      # age1<TODO-fill-after-issue-3>
      ,
      # Seeder service — generated during Stage-0 init, enables the Crossplane
      # workspace to automatically decrypt Stage-1 Tofu variables
      # without a human key on the Seeder. Added here after Seeder bootstrap
      # via a follow-up PR.
      # age1<TODO-fill-after-seeder-bootstrap>
